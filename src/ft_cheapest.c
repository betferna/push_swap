#include "push_swap.h"

ft_cheapest(t_stack **a, t_stack **b)
{
	int *count;
	
}

found_lowest(t_stack **stack)
{
	t_stack temp;
	if !stack	return
	while stack
		temp =  stack->value;

		while temp <	
	temp =  stack->value;

}